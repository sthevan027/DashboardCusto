services:
  - type: static
    name: dashboardcusto
    rootDir: web
    buildCommand: yarn install && yarn build
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: VITE_STANDALONE
        value: "1"
